package server;

import java.io.File;

import org.apache.log4j.Logger;

/**
 * The StorageEngine is responsible for reading and writing data on to disk.
 */
public class StorageEngine {
	private static final Logger logger = Logger.getLogger(StorageEngine.class);

	public static void writeData(File file, byte[] data) {

	}


}
